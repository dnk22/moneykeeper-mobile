import { StyleSheet } from 'react-native';

export const bankAccountListStyles = StyleSheet.create({
  totalBalanceContainer: {
    marginRight: 12,
    marginBottom: 12,
    paddingHorizontal: 16,
    paddingVertical: 14,
    borderRadius: 20,
  },
});

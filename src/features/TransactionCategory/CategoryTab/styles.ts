import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 10,
  },
  group: {
    borderRadius: 10,
    padding: 10,
    marginBottom: 10,
  },
  menu: {
    paddingBottom: 5,
    flexDirection: 'row',
    alignItems: 'center',
  },
  selectAs: {
    width: '40%',
  },
});

export default styles;

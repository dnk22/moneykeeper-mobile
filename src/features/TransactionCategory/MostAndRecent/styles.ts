import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  group: {
    borderRadius: 10,
    padding: 10,
    marginBottom: 10,
  },
  menu: {
    paddingBottom: 5,
    flexDirection: 'row',
    alignItems: 'center',
  },
  selectAs: {
    width: '40%',
  },
});

export default styles;

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 10,
  },
  group: {
    borderRadius: 10,
    padding: 10,
    marginBottom: 15,
  },
  mostRecent: {
    height: 100,
  },
  itemCategory: {
    maxHeight: 200,
  },
});

export default styles;

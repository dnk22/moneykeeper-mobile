import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  item: {
    paddingVertical: 15,
    flexDirection: 'row',
    alignItems: 'center',
    columnGap: 15,
  },
});

export default styles;
